import { Route, Switch } from 'react-router';

const Routes = () => {
  return (
    <Switch>
      <Route path='/contact-us' />
      <Route path='/' />
    </Switch>
  );
};

export default Routes;
